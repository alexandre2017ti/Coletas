# Somente desenvolvimento. Copie para .env e defina uma senha local aleatória.
POSTGRES_PASSWORD=
JWT_SIGNING_KEY=
# Recuperação opcional: valores reais somente no ambiente privado, nunca no Git.
RECOVERY_SMTP_HOST=
RECOVERY_SMTP_PORT=587
RECOVERY_SMTP_USERNAME=
RECOVERY_SMTP_PASSWORD=
RECOVERY_SMTP_FROM=
# HTTPS da tela web, incluindo ?view=reset-password e sem fragmento/token.
RECOVERY_RESET_URL=
